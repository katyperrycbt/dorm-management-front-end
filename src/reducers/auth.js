

const auth = (state = { authData: null}, action) => {
    switch (action.type) {
        case 'A':
            return state;
        default:
            return state;
    }
}

export default auth;


